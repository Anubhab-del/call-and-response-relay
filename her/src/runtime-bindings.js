// The only names the house borrows from the vendored runtime chunk.
// vendor/runtime.js is React 19 + Motion, built once and never hand-edited.
const React = y;
const ReactDOM = vu;
const jsx = I;
const motion = W;
const AnimatePresence = Nc;
